/*#include <iostream>
#include <string>
using namespace std;

// Wihtout using namespace, the below code would need
// std::cout 
// so using a namespace gives us access to the functions directly instead of through std::

int main() {

	// Chars store a single character.
	char dog = 'd';

	// Strings store multiple chars
	string player = "John";

	cout << "Player Name: " << player << endl;
	cout << "Lenth      : " << player.length() << endl;


	system("PAUSE");
	return 0;
}
*/